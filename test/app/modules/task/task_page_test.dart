import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TaskPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TaskPage(title: 'Task')));
    //  final titleFinder = find.text('Task');
    //  expect(titleFinder, findsOneWidget);
  });
}
