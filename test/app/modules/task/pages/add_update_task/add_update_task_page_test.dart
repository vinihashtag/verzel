import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AddUpdateTaskPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddUpdateTaskPage(title: 'AddUpdateTask')));
    //  final titleFinder = find.text('AddUpdateTask');
    //  expect(titleFinder, findsOneWidget);
  });
}
